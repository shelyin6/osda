# SQL 存储过程依赖分析工具：项目规则

## 1. 项目定位

本项目用于离线分析用户提供的 Oracle SQL/PLSQL 文件或代码文本，提取存储过程、函数、包体与数据库对象之间的表级读写依赖，辅助 DBA 在跑批失败或目标表数据异常时完成影响范围定位。

一期只提供**静态分析与证据展示**，不连接生产数据库、不自动执行存储过程、不自动执行重跑。

## 2. 一期范围

### 必须支持

- 输入：`.sql` 文件上传、多个 SQL 文件批量上传、代码文本粘贴。
- Oracle SQL/PLSQL：普通 SQL、`PROCEDURE`、`FUNCTION`、`PACKAGE BODY`。
- DML：`SELECT`、`INSERT`、`UPDATE`、`DELETE`、`MERGE`。
- 常见结构：表别名、JOIN、子查询、CTE、嵌套 SQL。
- 依赖查询：过程到表、表到过程、目标表上游追溯、目标表下游影响查询。
- 结果：关系表格、代码行号与 SQL 片段证据、解析告警、CSV 与 JSON 导出。
- 可在无网络环境安装、启动和运行。

### 不属于一期

- 直接连接 Oracle 数据库或读取生产元数据。
- 视图、同义词自动展开至基础表。
- 多数据库方言支持。
- 执行、调度、重跑存储过程或修改任何数据库数据。
- 将不确定的动态 SQL 当作确定依赖输出。

## 3. 依赖关系标准

核心模型为：

```text
来源程序单元 --[操作类型]--> 目标数据库对象
```

支持的操作类型：

- `READ`：`SELECT` 读取。
- `INSERT`：插入目标表。
- `UPDATE`：更新目标表。
- `DELETE`：删除目标表数据。
- `MERGE`：合并写入；必要时同时补充源表的 `READ` 关系。
- `READ_WRITE`：同一对象在同一逻辑中既读又写。
- `UNKNOWN`：无法静态确定。

每条关系至少保存以下字段：

```text
source_unit, source_type, source_file, source_location,
target_schema, target_object, target_type, operation,
confidence, sql_snippet, is_dynamic_sql, warning
```

`source_location` 至少应包含行号；能够提供列号时一并保留。对象名称默认按 Oracle 未加引号标识符规则规范化为大写；双引号标识符必须保留原始语义。

## 4. 解析原则

- 解析核心必须基于语法树（AST），不得以正则表达式作为 SQL/PLSQL 依赖分析的唯一或主要实现。
- 推荐使用 ANTLR Oracle PL/SQL Grammar；正则仅可用于预检查或辅助处理。
- CTE 名称、子查询别名、表别名不得误识别为物理表。
- 注释、字符串字面量内的关键字不得产生虚假的对象依赖。
- 每条依赖必须尽可能关联原始 SQL 片段和源码位置，作为 DBA 人工判断的证据。
- 解析不完整、语法不支持或对象不明确时，保留可用结果并输出结构化告警；不得因单段错误中断整个文件或任务。

## 5. 动态 SQL 规则

- `EXECUTE IMMEDIATE` 的完整常量 SQL 可尝试继续解析，但置信度不得高于 `MEDIUM`。
- 对象名由变量或表达式拼接时，创建 `UNKNOWN` 关系或告警，置信度为 `LOW`。
- 无法提取 SQL 内容的动态 SQL，必须保存其代码位置及“无法静态解析”的告警。
- 不得声称动态 SQL 的依赖关系完整或准确。

## 6. 目标表溯源规则

- 上游（`UPSTREAM`）：目标表 <- 写入该表的程序单元 <- 这些单元读取的表。
- 下游（`DOWNSTREAM`）：目标表 -> 读取该表的程序单元 -> 这些单元写入的表。
- 默认展示一跳关系；多跳展开必须限制最大深度并处理循环依赖。
- 图谱中必须保留“过程/函数/包体”节点，不得将过程逻辑直接臆断为纯表到表血缘。

## 7. 技术与部署约束

- 后端优先：Java 21 + Spring Boot。
- 前端优先：React + TypeScript。
- 单机部署默认使用 SQLite；多人内网部署可适配 PostgreSQL。
- 运行时不得依赖 CDN、外网 API、云端 SQL 解析、在线许可证校验或外部 AI 服务。
- 所有构建依赖必须版本固定并随离线发布包或镜像交付。
- 发布包必须包含依赖/许可证清单、版本号、校验和、数据库初始化脚本、部署及回滚说明。
- 输入 SQL 与分析结果只保存在用户本地或目标内网环境；日志不得打印完整敏感源码或凭据。

## 8. 测试要求

- 建立可版本控制的 Golden SQL 用例库：每个输入均有明确预期依赖和告警结果。
- 测试至少覆盖：普通 DML、JOIN、CTE、子查询、嵌套 SQL、别名、`MERGE`、过程/函数/包体、注释与字符串干扰、动态 SQL。
- 每次修改解析规则必须运行 Golden 测试，防止既有语法回归。
- 一期验收目标：静态表引用识别准确率不低于 95%，常规 DML 不得无告警漏检。
- 验证断网环境下安装、启动、分析和导出的完整流程。

## 9. 安全边界

- 默认以只读、静态方式处理用户输入。
- 禁止在未经明确授权的情况下连接数据库、读取数据库元数据或执行任何 SQL。
- 禁止实现“自动重跑”或“自动修复数据”功能；后续即使新增，也必须独立进行权限、审批、幂等性和事务设计。

## 10. 开发质量要求

- 保持解析、依赖抽取、图谱查询、文件管理和导出模块边界清晰。
- 依赖关系、告警和解析器版本必须可追溯。
- 对用户可见的结果中应显式展示置信度与不确定性，避免误导为生产结论。
- 新增功能必须先判断是否属于一期范围；超出范围时以可扩展接口或后续需求记录处理，不扩大一期交付范围。
